#!/bin/zsh

#------------------- Default settings ----------------------------------
alias \
	bld="make && sudo make clean install" \
	ccat="highlight --out-format=ansi" \
	cl="clear" \
	cp="cp -iv" \
	diff="diff --color=auto" \
	grep="grep --color=auto" \
	ka="killall" \
	live="live-server" \
	ll="ls -a --color=auto" \
	ls="ls -hN --color=auto --group-directories-first" \
	mkd="mkdir -pv" \
	mv="mv -iv" \
	p="sudo pacman" \
	rm="rm -vI" \

#------------------- Cli apps  -----------------------------------------
alias \
	f="$FILE" \
	sf="sudo $FILE" \
	v="$EDITOR" \
	sv="sudo $EDITOR" \
	z="$READER" \

#------------------- Git commands -------------------------------------
alias \
	g="git" \
	ga="git add" \
	gb="git branch" \
	gc="git commit" \
	gd="git diff" \
	gcl="git clone" \
	gp='git push'  \
	gP='git pull'  \
	gst="git status" \
	gitup="git add . && git commit -m 'Updates.' && git push" \

#------------------- Tmux commands -------------------------------------
alias \
	t="tmux -u" \
	ta="tmux -u a" \
	tls="tmux ls" \
	tks="tmux kill-session -t" \
